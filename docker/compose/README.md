This should be the basic idea of the project. Have containers and network setup to observe.

to start:

    $ sudo ./up.sh 

to close:

    $ sudo ./down.sh


to go in:

    $ sudo docker exec -it <name-of-container> /bin/ash

